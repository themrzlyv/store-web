version: "3.8"

services:
  app:
    container_name: store-web
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=${{ secrets.DATABASE_URL }}
